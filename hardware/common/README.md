# Common Hardware Assets

This directory contains reusable hardware assets shared by board revisions.

Use this directory for symbols, footprints, and 3D models that are not specific
to one board revision. Board-specific assets should stay inside the board
revision directory.
